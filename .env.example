# =============================================================================
# Craftwell Health Adviser — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# See docs/setup.md for full details.
# =============================================================================

# -----------------------------------------------------------------------------
# Required (Core) — needed for every page
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# -----------------------------------------------------------------------------
# Required (AI / Backend routes) — needed by chat, search, and ingestion APIs
# -----------------------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
ANTHROPIC_MODEL=claude-sonnet-4-6
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_INDEX=craftwell
VOYAGE_API_KEY=your-voyage-ai-api-key

# -----------------------------------------------------------------------------
# Google OAuth — configured in Supabase Dashboard, NOT here
# -----------------------------------------------------------------------------
# Google Sign-In is handled entirely by Supabase Auth. No env vars needed here.
#
# Setup steps:
#   1. Go to Google Cloud Console > APIs & Services > Credentials
#   2. Create an OAuth 2.0 Client ID (Web application)
#   3. Set Authorized redirect URI to:
#        https://<your-project-ref>.supabase.co/auth/v1/callback
#   4. Copy the Client ID and Client Secret
#   5. In Supabase Dashboard > Authentication > Providers > Google:
#      - Enable the Google provider
#      - Paste Client ID and Client Secret
#   6. For local development, also add http://localhost:54321/auth/v1/callback
#      as an authorized redirect URI in Google Cloud Console
#
# No additional env vars are required in this file — Supabase stores the
# Google credentials securely in its own configuration.

# Apple Sign-In must remain hidden until the Apple provider is enabled in
# Supabase with a valid Services ID and secret.
NEXT_PUBLIC_APPLE_AUTH_ENABLED=false

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
ADMIN_API_KEY=your-admin-bearer-token
CRON_SECRET=use-at-least-32-random-characters
# NCBI_CONTACT_EMAIL=operator@example.com
# NCBI_API_KEY=your-optional-ncbi-api-key
# CROSSREF_CONTACT_EMAIL=operator@example.com
# OPEN_LIBRARY_CONTACT_EMAIL=operator@example.com
# Reserved for the reviewed metadata-only adapter; legacy description copying is disabled.
# YOUTUBE_API_KEY=your-youtube-data-api-key
NEXT_PUBLIC_APP_URL=http://localhost:3000
LOG_LEVEL=debug
